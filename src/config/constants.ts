export const OWNER_ID = 'local' // Local user for offline-first

export const SIDEBAR_CONFIG = {
  DEFAULT_WIDTH: 280,
  MIN_WIDTH: 200,
  MAX_WIDTH: 400
} as const

export const AUTOSAVE_DELAY = 1000 // 1 second

export const EDITOR_FONT_SIZES = {
  small: '14px',
  medium: '16px',
  large: '18px'
} as const
